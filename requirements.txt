--extra-index-url https://download.pytorch.org/whl/cpu

# Environment variables
python-dotenv>=1.0.0

# Playwright (Browser engine)
playwright>=1.40.0

# Vision, Array and Tensor processing (Foveated tokenization & OmniParser)
numpy>=1.24.0
scipy>=1.10.0
Pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0

# LLM integration (Slow Planner / Fast Edge)
transformers>=4.30.0
openai>=1.0.0
einops>=0.7.0
bitsandbytes>=0.41.0
accelerate>=0.20.3

# Testing framework
pytest>=8.0.0
